# Node modules / temp files
node_modules/
.DS_Store
Thumbs.db
*.log
# Graphify output: Ignore build state and logs, but keep main graph file for team context
graphify-out/*
!graphify-out/graph.json

# Built packages (stored locally, not in git source repo to avoid false-positive AV warnings)
*.zip
*.xpi

# Chrome Web Store / publishing secrets and local state
.cws_refresh_token
.cws_item_id
.env
.env.*
